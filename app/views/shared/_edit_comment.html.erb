  <% form_tag :action => :update, :id => c.id do%>
    <hr>
    <b>Edit Comment:</b><br>
	Title: <%= text_field_tag 'comment[title]', c.title, :size => 50 %><br>
    Text:<%= text_area_tag 'comment[comment]', c.comment, :cols=>60, :rows=>3 %>
    <hr>
	<%= submit_tag "Upd comment"%>
  <% end %>
